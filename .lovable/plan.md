## Ação

Conceder acesso total à conta **fbcfarias@gmail.com** (`eb65dc55-5944-430a-808d-40ef813d0193`):

1. Inserir role `admin` em `user_roles`.
2. Inserir/atualizar linha em `subscriptions` com `status = 'active'` e `lifetime_access = true` (acesso vitalício, sem cobrança Stripe).

Sem mudanças de código ou schema — apenas data ops na base.
